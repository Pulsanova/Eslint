{
    "private": true,
    "scripts": {
        "lint": "eslint .",
        "bump-all": "lerna version --no-git-tag-version --no-push",
        "publish-all": "lerna publish from-package"
    },
    "devDependencies": {
        "@pulsanova/eslint-config-base": "file:./packages/base",
        "eslint": "^7.32.0",
        "lerna": "^4.0.0"
    }
}
